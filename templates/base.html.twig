<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Boutique de Montres{% endblock %}</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
            <link rel="stylesheet" href="{{ asset('css/app.css') }}">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </head>
    <body class="d-flex flex-column min-vh-100">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}" style="font-family: 'Playfair Display', serif;">WatchHouse</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_catalogue') }}">Catalogue</a>
                        </li>
                    </ul>
                    <div class="navbar-nav ms-auto">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="nav-link" href="{{ path('admin_dashboard') }}">Dashboard</a>
                            <a class="nav-link" href="{{ path('app_montre_index') }}">Gérer les montres</a>
                            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                        {% else %}
                             <a class="nav-link" href="{{ path('app_login') }}">Connexion Admin</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>
        
        <main class="flex-grow-1">
            {% block body %}{% endblock %}
        </main>

        <footer class="footer">
            <div class="container">
                <div class="row">
                    <div class="col-md-4">
                        <h5 style="font-family: 'Playfair Display', serif;">WatchHouse</h5>
                        <p>La référence des montres de luxe et de collection.</p>
                    </div>
                    <div class="col-md-4">
                        <h5>Navigation</h5>
                        <ul class="list-unstyled">
                            <li><a href="{{ path('app_home') }}">Accueil</a></li>
                            <li><a href="{{ path('app_catalogue') }}">Catalogue</a></li>
                        </ul>
                    </div>
                    <div class="col-md-4">
                        <h5>Contact</h5>
                        <p>Email: contact@watchhouse.com</p>
                    </div>
                </div>
                <div class="text-center pt-3" style="border-top: 1px solid #495057;">
                    <p>&copy; {{ "now"|date("Y") }} WatchHouse. Tous droits réservés.</p>
                </div>
            </div>
        </footer>
    </body>
</html>
